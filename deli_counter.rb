def line(arr)
  
  arr = []
  
  if arr.length > 0
    
    arr.each.with_index(1).map { |name, i| }
      
    return "Line is currently: #{i}. #{name}."
    
  else
    
    puts "The line is currently empty."
    
  end

end
